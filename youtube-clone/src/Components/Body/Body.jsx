import { SideBar, MainContainer } from "../index";

const Body = () => {
    return (
        <div className="flex w-full">
            <SideBar />
            <MainContainer />
        </div>
    );
};

export default Body;